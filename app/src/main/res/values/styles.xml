<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_white</item>
        <item name="colorPrimaryDark">@color/color_dark_gray</item>
        <item name="colorAccent">@color/color_light_pink</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="toolbarStyle">@style/Widget.AppCompat.Toolbar</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Tabs" parent="Base.Widget.Design.TabLayout">
        <item name="tabBackground">@color/color_white</item>
        <item name="tabIndicatorColor">@color/color_dark_pink</item>
        <item name="tabSelectedTextColor">@color/color_dark_pink</item>
        <item name="tabTextAppearance">@color/color_dark_gray</item>
    </style>

    <style name="CustomSpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/ic_overflow</item>
    </style>

    <style name="ToolbarText" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/color_default</item>
        <item name="android:background">@color/color_white</item>
    </style>

    <style name="Widget.AppCompat.Toolbar." parent="Widget.AppCompat.Toolbar.">
        <item name="android:background">@color/color_white</item>
        <item name="android:elevation">2dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
    <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Widget.Design.FloatingActionButton." parent="Widget.Design.FloatingActionButton.">
        <item name="android:icon">@drawable/ic_add</item>
        <item name="android:shadowColor">@color/color_dark_gray</item>
    </style>

    <style name="FAB" parent="@style/Widget.Design.FloatingActionButton.">
    </style>

    <style name="scrollbar_style">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_track</item>
        <item name="android:scrollbarSize">8dp</item>
        <item name="android:scrollbarFadeDuration">800</item>
        <item name="android:scrollbarDefaultDelayBeforeFade">500</item>
    </style>

</resources>
